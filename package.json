{
  "name": "clubcal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "echo 'No TypeScript config — skipping typecheck. Install types for JS files or add tsconfig.'"
  },
  "devDependencies": {
    "eslint": "^9.25.1",
    "globals": "^16.0.0",
    "happy-dom": "^20.9.0",
    "prettier": "3.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.3"
  }
}
